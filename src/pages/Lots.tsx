import LotsBox from "../components/LotsBox";

const Lots = () => {
  return (
    <div
      style={{
        display: "flex",
        margin: "0 auto",
        alignItems: "center",
        maxWidth: "860px",
      }}
    >
      <LotsBox />
    </div>
  );
};

export default Lots;
